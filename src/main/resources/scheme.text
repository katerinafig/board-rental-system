

┌───────────────────────────────────────────────────────────────┐
│                   ЭКОСИСТЕМА OPENAPI                          │
├───────────────────────────────────────────────────────────────┤
│                                                               │
│  ┌─────────────────┐    реализует     ┌────────────────────┐  │
│  │   Спецификация  │ ◄─────────────── │   OpenAPI (OAS)    │  │
│  │    OpenAPI      │                  │   СТАНДАРТ         │  │
│  │   (формат .yaml,│                  │                    │  │
│  │     .json)      │ ───────────────► │ Формальный стандарт│  │
│  └─────────────────┘   описывает      │ описания REST API  │  │
│         ▲                             └────────────────────┘  │
│         │                                        ▲            │
│         │                                        │            │
│  ┌──────┴─────────┐                    ┌─────────┴──────────┐ │
│  │   Инструменты  │                    │   Реализации       │ │
│  │   Swagger      │                    │   для фреймворков  │ │
│  │                │                    │                    │ │
│  ├────────────────┤                    ├────────────────────┤ │
│  │ Swagger UI     │◄─── использует ────│   springdoc        │ │
│  │ (визуализация) │                    │   (для Spring)     │ │
│  ├────────────────┤                    ├────────────────────┤ │
│  │ Swagger Editor │                    │   swagger-js       │ │
│  │ (редактор)     │                    │   (для Node.js)    │ │
│  ├────────────────┤                    ├────────────────────┤ │
│  │ Swagger Codegen│                    │   FastAPI          │ │
│  │ (генерация     │                    │   (для Python)     │ │
│  │  кода)         │                    └────────────────────┘ │
│  └────────────────┘                                           │
│                                                               │
└───────────────────────────────────────────────────────────────┘







┌─────────────────────┐    генерирует    ┌─────────────────┐
│   Spring Boot       │ ────────────────►│  OpenAPI Spec   │
│   Приложение        │                  │  (openapi.yaml) │
│                     │ ◄────────────────│                 │
│  - @RestController  │   использует     └─────────────────┘
│  - @GetMapping      │                         │
│  - @Schema          │                         │ предоставляет
└─────────────────────┘                         ▼
       │                            ┌─────────────────┐
       │ использует                 │   Swagger UI    │
       ▼                            │                 │
┌─────────────────┐                 │ - Документация  │
│   springdoc     │ ◄───────────────│ - Тестирование  │
│   библиотека    │   отображает    │ - Try it out    │
└─────────────────┘                 └─────────────────┘
